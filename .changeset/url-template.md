---
"traforo": minor
---

Support configurable public tunnel URL templates, including nested hostnames such as `https://{id}.tunnel.example.com`, while preserving the legacy `{id}-tunnel` hostname format.
